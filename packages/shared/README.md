# packages/shared

טיפוסים ולוגיקה משותפים בין ה-desktop ל-workers.

## תוכן מתוכנן
- טיפוסי מודל הנתונים: `StudioProfile`, `Session`, `MediaAsset`, `TimelineEvent`,
  `EditJob`, `DeliverableTemplate` (ראה [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md) §7).
- קבועים ו-enums של מצבי ה-State Machine.
- סכמות ולידציה (zod) ל-Studio Profiles ול-job payloads.
